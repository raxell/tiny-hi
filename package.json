{
  "scripts": {
    "build": "tsc",
    "test": "npm run build && jest --testPathPattern .test.js$",
    "start": "npm run build && node build/index.js"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/jest": "^26.0.14",
    "jest": "^26.4.2",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  }
}
